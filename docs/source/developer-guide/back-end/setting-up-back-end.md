# Setting Up Back-End

## Pre-requisites

## Setting Up MongoDB

## Setting Up MinIO

## Installing Dependencies

To install, change to the `back-end` directory and run:

```bash
python -m venv venv
source venv/bin/activate
poetry install
```
